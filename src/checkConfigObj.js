// @flow
// check to see if the provided configuration object is valid. not done in production

import type { ConfigurationType } from "./uniformed.flow";

const checkConfigObj = (configObj: ConfigurationType) => true; // TODO

export default checkConfigObj;
